This is my site, yo!

Build and take down the site in a local environment:
```
docker compose up site_dev -d
docker compose down site_dev
```
Build and take down the site in a porduction environment:
```
docker compose up site_prod -d
docker compose down site_prod
```

Site can be found at `http://localhost:3000/`